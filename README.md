# GPT / LLM Interview Evaluation Module

This module evaluates interview answers using an LLM and returns structured scores.

## Features
- Meaning-based evaluation
- Bias-aware scoring
- JSON-only output
- FastAPI integration

## Setup

1. Create virtual environment
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
